const cartInitState = {
    cartData: [],
    cartTotal: 0,
}

const cartReducers = (state = cartInitState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default cartReducers;