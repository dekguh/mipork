import Navigation from '../../organisms/Navigation';

const index = ({ children }) => {
    return (
        <>
            {children}
            <Navigation />
        </>
    )
}

export default index
